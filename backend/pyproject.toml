[project]
name = "text-graph-mvp-backend"
version = "0.1.0"
description = "Backend for a text relationship graph MVP"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.115.0",
  "httpx==0.28.1",
  "pydantic==2.9.2",
  "uvicorn[standard]==0.30.6",
]
